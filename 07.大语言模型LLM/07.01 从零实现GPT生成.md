---
created: 2026-05-26
updated: 2026-05-26
tags:
  - llm
  - gpt
  - transformer
  - practice
  - phase5
type: practice
phase: 5
difficulty: ⭐⭐⭐⭐
---

# 从零实现 GPT 生成

> 跟着 Karpathy 的思路，从 Transformer 到字符级 GPT，用几百行代码跑通一个能"说话"的模型。

---

## 🎯 练习目标

1. 理解 Decoder-Only 架构与 Encoder-Decoder 的区别
2. 实现 Causal Self-Attention（只看左边、不看右边）
3. 掌握自回归生成的采样策略
4. 训练一个微型 GPT 并在 Shakespeare 文本上生成

---

## 1️⃣ Decoder-Only vs Encoder-Decoder

```
Encoder-Decoder (BERT/T5):       Decoder-Only (GPT):
  源序列 → [Encoder] → 上下文      全部序列 → [Decoder×N] → 下一个词
                          ↓
  目标序列 ← [Decoder] ← ┘

GPT 的哲学：把一切都当成"下一个词预测"。翻译？源语言 + 目标语言拼一起，预测。
```

```python
import torch
import torch.nn as nn
import torch.nn.functional as F
import math

# Causal Mask: 确保第 i 个 token 只能看到前 i-1 个 token
def create_causal_mask(seq_len):
    mask = torch.tril(torch.ones(seq_len, seq_len))  # 下三角 = 1
    return mask.view(1, 1, seq_len, seq_len)

# 可视化
mask = create_causal_mask(8)
print("Causal Mask (1=可见, 0=遮蔽):")
print(mask[0, 0].int())
print("\n💡 第 4 个 token 只能看到前 3 个 → 自回归生成的本质")
```

---

## 2️⃣ Causal Self-Attention

```python
class CausalSelfAttention(nn.Module):
    """和 05.03 的 Self-Attention 唯一的区别：加了 causal mask"""
    def __init__(self, d_model, num_heads, dropout=0.1):
        super().__init__()
        self.num_heads = num_heads
        self.d_k = d_model // num_heads
        
        self.W_Q = nn.Linear(d_model, d_model)
        self.W_K = nn.Linear(d_model, d_model)
        self.W_V = nn.Linear(d_model, d_model)
        self.W_O = nn.Linear(d_model, d_model)
        self.dropout = nn.Dropout(dropout)
    
    def forward(self, x):
        B, T, C = x.shape
        
        Q = self.W_Q(x).view(B, T, self.num_heads, self.d_k).transpose(1, 2)
        K = self.W_K(x).view(B, T, self.num_heads, self.d_k).transpose(1, 2)
        V = self.W_V(x).view(B, T, self.num_heads, self.d_k).transpose(1, 2)
        
        # Scaled Dot-Product + Causal Mask
        scores = (Q @ K.transpose(-2, -1)) / math.sqrt(self.d_k)
        causal_mask = torch.tril(torch.ones(T, T, device=x.device)).view(1, 1, T, T)
        scores = scores.masked_fill(causal_mask == 0, float('-inf'))
        
        attn = F.softmax(scores, dim=-1)
        attn = self.dropout(attn)
        
        out = (attn @ V).transpose(1, 2).contiguous().view(B, T, C)
        return self.W_O(out)
```

---

## 3️⃣ 🔥 微型 GPT 完整实现

```python
class GPTBlock(nn.Module):
    """Transformer Decoder Block"""
    def __init__(self, d_model, num_heads, d_ff, dropout=0.1):
        super().__init__()
        self.attn = CausalSelfAttention(d_model, num_heads, dropout)
        self.ffn = nn.Sequential(
            nn.Linear(d_model, d_ff),
            nn.GELU(),
            nn.Linear(d_ff, d_model),
            nn.Dropout(dropout),
        )
        self.ln1 = nn.LayerNorm(d_model)
        self.ln2 = nn.LayerNorm(d_model)
    
    def forward(self, x):
        x = x + self.attn(self.ln1(x))
        x = x + self.ffn(self.ln2(x))
        return x

class MiniGPT(nn.Module):
    def __init__(self, vocab_size, d_model=256, num_heads=4, num_layers=4, d_ff=512, block_size=128):
        super().__init__()
        self.token_embedding = nn.Embedding(vocab_size, d_model)
        self.position_embedding = nn.Embedding(block_size, d_model)
        self.blocks = nn.Sequential(*[
            GPTBlock(d_model, num_heads, d_ff) for _ in range(num_layers)
        ])
        self.ln_final = nn.LayerNorm(d_model)
        self.lm_head = nn.Linear(d_model, vocab_size)  # 语言模型头
        self.block_size = block_size
    
    def forward(self, idx):
        B, T = idx.shape
        tok_emb = self.token_embedding(idx)
        pos_emb = self.position_embedding(torch.arange(T, device=idx.device))
        x = tok_emb + pos_emb
        x = self.blocks(x)
        x = self.ln_final(x)
        return self.lm_head(x)  # [B, T, vocab_size] — 每个位置预测下一个词
    
    @torch.no_grad()
    def generate(self, idx, max_new_tokens, temperature=1.0, top_k=None):
        """自回归生成"""
        for _ in range(max_new_tokens):
            idx_cond = idx[:, -self.block_size:]  # 截断到 block_size
            logits = self(idx_cond)
            logits = logits[:, -1, :] / temperature  # 只取最后一个位置
            
            if top_k is not None:
                v, _ = torch.topk(logits, top_k)
                logits[logits < v[:, [-1]]] = float('-inf')
            
            probs = F.softmax(logits, dim=-1)
            idx_next = torch.multinomial(probs, num_samples=1)
            idx = torch.cat([idx, idx_next], dim=1)
        
        return idx

model = MiniGPT(vocab_size=100)
x = torch.randint(0, 100, (2, 64))
out = model(x)
print(f"MiniGPT: {x.shape} → {out.shape}")
print(f"参数量: {sum(p.numel() for p in model.parameters()):,}")
```

---

## 4️⃣ 采样策略对比

```python
def sample(logits, temperature=1.0, top_k=0, top_p=0.9):
    logits = logits / temperature
    probs = F.softmax(logits, dim=-1)
    
    if top_k > 0:
        # 只保留 top-k 个最大概率
        top_k_probs, _ = torch.topk(probs, top_k)
        min_top_k = top_k_probs[:, -1].unsqueeze(-1)
        probs[probs < min_top_k] = 0
        probs = probs / probs.sum(dim=-1, keepdim=True)
    
    if top_p > 0:
        # Nucleus sampling: 保留累积概率达到 p 的词
        sorted_probs, sorted_idx = probs.sort(descending=True)
        cumsum = sorted_probs.cumsum(dim=-1)
        mask = cumsum - sorted_probs > top_p
        sorted_probs[mask] = 0
        sorted_probs = sorted_probs / sorted_probs.sum(dim=-1, keepdim=True)
        probs = torch.zeros_like(probs).scatter_(-1, sorted_idx, sorted_probs)
    
    return torch.multinomial(probs, 1)

print("Temperature=0.1 → 几乎总是选最大概率 → 保守/重复")
print("Temperature=1.0 → 按原始概率 → 平衡")
print("Temperature=2.0 → 概率更均匀 → 更有创意但可能胡说")
print("top_k=40 → 只从 Top 40 个词中选 → 防止低概率噪声")
```

---

## 5️⃣ Karpathy 风格的字符级训练

```python
# 下载 Shakespeare 文本
# !wget https://raw.githubusercontent.com/karpathy/char-rnn/master/data/tinyshakespeare/input.txt

with open('input.txt', 'r') as f:
    text = f.read()

chars = sorted(list(set(text)))
vocab_size = len(chars)
stoi = {ch: i for i, ch in enumerate(chars)}  # char → id
itos = {i: ch for i, ch in enumerate(chars)}  # id → char

# 编码 + 解码
def encode(s): return [stoi[c] for c in s]
def decode(ids): return ''.join([itos[i] for i in ids])

data = torch.tensor(encode(text), dtype=torch.long)
n = int(0.9 * len(data))
train_data, val_data = data[:n], data[n:]

# 训练一个 epoch 后生成
model = MiniGPT(vocab_size=vocab_size, d_model=128, num_heads=4, 
                num_layers=3, d_ff=256, block_size=64)
# ... 训练循环 ...
# context = torch.zeros((1, 1), dtype=torch.long)
# print(decode(model.generate(context, max_new_tokens=500)[0].tolist()))

print("💡 这就是 Andrej Karpathy 'Let's build GPT from scratch' 的精简版")
print("   完整视频: https://www.youtube.com/watch?v=kCc8FmEb1nY")
```

---

## ✅ 完成标准

- [ ] 能解释 Causal Mask 为什么是下三角矩阵
- [ ] 能从零写出 MiniGPT 的 forward 和 generate 函数
- [ ] 理解 Temperature / Top-k / Top-p 的区别
- [ ] 训练一个字符级 GPT 并生成可读（尽管胡言乱语）的文本

---

## 📚 推荐资源

- [Karpathy: Let's build GPT from scratch](https://www.youtube.com/watch?v=kCc8FmEb1nY) ⭐
- [Karpathy: nanoGPT](https://github.com/karpathy/nanoGPT)
- [Lilian Weng: The Transformer Family](https://lilianweng.github.io/posts/2023-01-27-the-transformer-family-v2/)

---

## 🔗 相关笔记

- [[07.大语言模型LLM/07.00 Phase5-大语言模型|◀ 返回 Phase 5]]
- [[05.NLP/05.03 Transformer从零实现]]
- [[07.大语言模型LLM/07.03 RAG实战：搭建知识库问答系统]]

---
created: 2026-05-26
updated: 2026-05-26
tags:
  - mlops
  - deployment
  - engineering
  - practice
  - phase6
type: practice
phase: 6
difficulty: ⭐⭐⭐⭐⭐
---

# MLOps 实战：模型部署流水线

> 从"Jupyter 里能跑"到"生产环境稳定服务"。MLOps = ML + DevOps。

---

## 🎯 练习目标

1. 理解 MLOps 的完整生命周期
2. 用 MLflow 管理实验
3. 用 Docker + FastAPI 部署模型
4. 搭建监控和告警

---

## 1️⃣ MLOps 全景

```
    实验          训练          部署          监控
    ─────────────────────────────────────────────→
    MLflow       DVC          Docker        Prometheus
    W&B          DeepSpeed    FastAPI       Grafana
                 DDP          Triton        Alerting
```

```python
# pip install mlflow
import mlflow
import mlflow.sklearn
from sklearn.ensemble import RandomForestClassifier

mlflow.set_experiment("my-first-experiment")

with mlflow.start_run():
    # 记录超参数
    mlflow.log_param("n_estimators", 100)
    mlflow.log_param("max_depth", 5)
    
    # 训练
    model = RandomForestClassifier(n_estimators=100, max_depth=5)
    model.fit(X_train, y_train)
    
    # 记录指标
    acc = model.score(X_test, y_test)
    mlflow.log_metric("accuracy", acc)
    
    # 保存模型
    mlflow.sklearn.log_model(model, "model")
    
    print(f"Run ID: {mlflow.active_run().info.run_id}")
    print(f"Accuracy: {acc:.3f}")

# 启动 UI: mlflow ui → http://localhost:5000
```

---

## 2️⃣ 模型服务：FastAPI + Docker

```python
# app.py
from fastapi import FastAPI
from pydantic import BaseModel
import mlflow
import numpy as np

app = FastAPI()
model = mlflow.sklearn.load_model("models:/my-model/latest")

class PredictRequest(BaseModel):
    features: list[float]

@app.post("/predict")
async def predict(req: PredictRequest):
    X = np.array(req.features).reshape(1, -1)
    pred = model.predict(X).tolist()
    return {"prediction": pred}

@app.get("/health")
async def health():
    return {"status": "ok"}
```

```dockerfile
# Dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
EXPOSE 8000
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
```

```bash
docker build -t my-model-service .
docker run -p 8000:8000 my-model-service
# curl -X POST http://localhost:8000/predict -d '{"features": [1,2,3,4]}'
```

---

## 3️⃣ 实验管理最佳实践

```python
# MLflow 自动记录（一行代码！）
mlflow.sklearn.autolog()

# 训练时自动记录：参数、指标、模型、环境信息
model = RandomForestClassifier()
model.fit(X_train, y_train)

# 对比实验
# 打开 MLflow UI → 选两个 Run → Compare → 看参数和指标差异
```

---

## 4️⃣ 模型监控

```python
# 数据漂移检测
from evidently.report import Report
from evidently.metric_preset import DataDriftPreset

# 对比训练数据和线上数据的分布
report = Report(metrics=[DataDriftPreset()])
report.run(reference_data=train_df, current_data=production_df)
report.save_html("drift_report.html")

# 指标监控
# - 预测延迟 p50/p99
# - 错误率
# - 输入特征分布漂移
# - 输出分布漂移
```

---

## ✅ 完成标准

- [ ] 能用 MLflow 追踪至少 5 组实验
- [ ] 能 Docker 化一个模型服务
- [ ] 理解 CI/CD for ML 的流程
- [ ] 知道数据漂移和模型退化的概念

---

## 📚 推荐资源

- [MLflow 文档](https://mlflow.org/)
- [Made With ML: MLOps Course](https://madewithml.com/)
- [CS 329S: Machine Learning Systems Design](https://stanford-cs329s.github.io/)

---

## 🔗 相关笔记

- [[08.前沿与工程化/08.00 Phase6-前沿与工程化|◀ 返回 Phase 6]]
- [[07.大语言模型LLM/07.00 Phase5-大语言模型]]

---
created: 2026-05-26
updated: 2026-05-26
tags:
  - advanced
  - multimodal
  - clip
  - practice
  - phase6
type: practice
phase: 6
difficulty: ⭐⭐⭐⭐⭐
---

# 多模态应用实战

> 图像 + 文本 + 语音 = 多模态。CLIP 是连接图文世界的桥梁。

---

## 🎯 练习目标

1. 理解 CLIP 的双塔架构（图像 Encoder + 文本 Encoder）
2. 用 CLIP 做零样本图像分类
3. 图文搜索（以文搜图、以图搜文）
4. 了解 LLaVA 等多模态对话模型

---

## 1️⃣ CLIP 原理

```python
import torch
import torch.nn as nn
import matplotlib.pyplot as plt
from PIL import Image

# CLIP = 图像 Encoder（ViT/ResNet） + 文本 Encoder（Transformer）
# 训练目标：让匹配的图文对向量尽可能相似

# 伪架构
"""
Image → [Vision Encoder] → I₁, I₂, ..., Iₙ
Text  → [Text Encoder]   → T₁, T₂, ..., Tₙ

Similarity Matrix:
        T₁  T₂  ... Tₙ
    I₁ [0.9 0.1 ... 0.0]  ← I₁和T₁匹配 → 对角线高
    I₂ [0.1 0.8 ... 0.0]
    ...
    
Loss = CrossEntropy(对角线)
"""
```

---

## 2️⃣ 🔥 CLIP 零样本分类

```python
# pip install open-clip-torch
import open_clip

model, _, preprocess = open_clip.create_model_and_transforms('ViT-B-32', pretrained='laion2b_s34b_b79k')
tokenizer = open_clip.get_tokenizer('ViT-B-32')

# 零样本分类：不需要训练数据！
image = preprocess(Image.open("cat.jpg")).unsqueeze(0)
texts = tokenizer([
    "a photo of a cat",
    "a photo of a dog", 
    "a photo of a bird",
    "a photo of a car",
])

with torch.no_grad():
    image_features = model.encode_image(image)
    text_features = model.encode_text(texts)
    
    # 归一化
    image_features /= image_features.norm(dim=-1, keepdim=True)
    text_features /= text_features.norm(dim=-1, keepdim=True)
    
    # 相似度
    similarity = (100.0 * image_features @ text_features.T).softmax(dim=-1)
    
    for i, text in enumerate(["cat", "dog", "bird", "car"]):
        print(f"{text}: {similarity[0, i]:.3f}")

print("\n💡 CLIP 的魔法：不需要任何分类训练数据！")
print("   只要给出类别的自然语言描述 → 自动匹配最相似的")
```

---

## 3️⃣ 图文互搜

```python
# 以文搜图
query = "a sunset over the ocean"
query_features = model.encode_text(tokenizer([query]))
query_features /= query_features.norm(dim=-1, keepdim=True)

# 对图片库每一张图算相似度 → 排序 → 返回 Top-K
# for img in image_library:
#     score = (query_features @ img_features.T).item()
```

---

## 4️⃣ LLaVA：多模态对话

```python
# LLaVA = Vision Encoder + LLM
# 让 LLM 能"看懂"图片并回答关于图片的问题

# from transformers import LlavaForConditionalGeneration
# model = LlavaForConditionalGeneration.from_pretrained("llava-hf/llava-1.5-7b-hf")
# 输入: 图片 + "这张图片里有什么？"
# 输出: "图片中有一只橙色的猫坐在窗台上..."

print("💡 多模态的终极形态：")
print("   输入 = 任意模态（图/文/音/视频）")
print("   输出 = 任意模态")
print("   GPT-4V / Gemini / Claude 3 都是这个方向")
```

---

## ✅ 完成标准

- [ ] 理解 CLIP 的双塔架构和对比学习目标
- [ ] 能用 CLIP 完成零样本图像分类
- [ ] 知道 Stable Diffusion 的文本编码器就是用的 CLIP

---

## 🔗 相关笔记

- [[08.前沿与工程化/08.00 Phase6-前沿与工程化|◀ 返回 Phase 6]]
- [[06.计算机视觉/06.00 计算机视觉总览]]
- [[07.大语言模型LLM/07.00 Phase5-大语言模型]]

---
created: 2026-05-26
updated: 2026-05-26
tags:
  - project
  - phase5
  - llm
  - deployment
type: project
phase: 5
difficulty: ⭐⭐⭐⭐
status: 📝 待完成
---

# P17-模型部署

> 用 Ollama 本地部署开源模型，用 FastAPI 封装 API，用 Docker 容器化。

---

## 🎯 项目目标

1. 本地部署一个开源 LLM
2. 构建 REST API 服务
3. Docker 容器化
4. 理解部署和调试的完整流程

---

## 📋 任务步骤

### Step 1：Ollama 本地部署
```bash
# 安装 Ollama
curl -fsSL https://ollama.ai/install.sh | sh

# 拉取模型
ollama pull qwen2:0.5b
ollama pull llama3:8b

# 测试
ollama run qwen2:0.5b "你好，介绍一下你自己"
```

### Step 2：FastAPI 服务
```python
from fastapi import FastAPI
from pydantic import BaseModel
import ollama

app = FastAPI()

class Query(BaseModel):
    prompt: str
    temperature: float = 0.7

@app.post("/generate")
async def generate(query: Query):
    response = ollama.chat(model="qwen2:0.5b", messages=[
        {"role": "user", "content": query.prompt}
    ])
    return {"response": response["message"]["content"]}

# uvicorn app:app --host 0.0.0.0 --port 8000
```

### Step 3：Docker 容器化
```dockerfile
FROM python:3.11-slim
RUN pip install fastapi uvicorn ollama
COPY app.py .
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
```

### Step 4：性能测试
- [ ] 用 wrk/locust 测 QPS
- [ ] 记录不同并发下的延迟
- [ ] 尝试用 vLLM 替代 Ollama 提升吞吐

---

## 🏆 验收标准

- [ ] 能通过 API 调用本地模型
- [ ] Docker 一键启动
- [ ] 单次推理 < 3 秒（0.5B 模型）
- [ ] 性能测试报告

---

## 🔗 相关笔记

- [[07.大语言模型LLM/07.04 LoRA微调实战：领域适配]]
- [[07.大语言模型LLM/07.00 Phase5-大语言模型|◀ 返回 Phase 5]]
